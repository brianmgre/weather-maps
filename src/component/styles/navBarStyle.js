export const styles = theme => ({
  root: {
    backgroundColor: theme.palette.secondary.main
  },

  logoText: {
    color: "white",
    "&:hover": {
      cursor: "pointer"
    }
  }
});
