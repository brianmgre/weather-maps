export const styles = theme => ({
  root: {
    width: "100%"
  },
  switches: {
    width: "100%",
    textAlign: "center"
  },

  currentWeather: {
    textAlign: "center",
    marginTop: 20
  }
});
